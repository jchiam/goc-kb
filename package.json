{
  "private": true,
  "scripts": {
    "docker:build": "cross-env BUILDX_NO_DEFAULT_ATTESTATIONS=1 docker compose build",
    "docker:up": "cross-env BUILDX_NO_DEFAULT_ATTESTATIONS=1 docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/node-cron": "^3.0.11",
    "cross-env": "^7.0.3"
  }
}
